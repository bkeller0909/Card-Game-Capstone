using UnityEngine;

public class HandSelect : MonoBehaviour
{
    private GameObject fingerObject;
    private FingerSelect fingerSelect;

    bool isHovered;
    bool isSelected;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
